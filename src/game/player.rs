use web_sys::HtmlImageElement;

pub struct Player {
    pub x: f64,
    pub y: f64,
    pub width: f64,
    pub height: f64,
    pub speed: f64,
    pub image: HtmlImageElement, // プレイヤーの画像
}

impl Player {
    // 必要に応じてメソッドを追加
}
